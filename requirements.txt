# ---- Core orchestration ----
langgraph>=0.2.60
langchain>=0.3.0
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0   # RecursiveCharacterTextSplitter (chunker)

# ---- LLM (router + agents) ----
langchain-openai>=0.2.0           # OpenRouter (Gemma) via OpenAI-compatible API

# ---- Knowledge Agent: embeddings + vector store ----
langchain-huggingface>=0.1.2      # intfloat/multilingual-e5-base wrapper
sentence-transformers>=3.0.0      # pulls in torch; downloads e5 model once
langchain-chroma>=0.1.4
chromadb>=0.5.0
pypdf>=5.0.0                       # PDF loading (Rahaf_CV.pdf, etc.)

# ---- Research Agent: keyless web search ----
ddgs>=6.0.0                        # maintained DuckDuckGo search (no API key)

# ---- Workspace Agent: Filesystem MCP ----
langchain-mcp-adapters>=0.1.0
mcp>=1.1.0

# ---- Config / schemas ----
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# ---- Interface ----
streamlit>=1.40.0

# ---- Voice layer (bonus) ----
groq>=0.11.0          # cloud speech-to-text (Whisper)
edge-tts>=6.1.0       # cloud text-to-speech (no key)
